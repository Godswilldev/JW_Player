// import Player from "./api";
//import { Api } from "./api";

// const loadForm = document.querySelector(".load");
// const forms = document.querySelectorAll("form");

// forms.forEach((form) =>
//   form.addEventListener("click", (evt) => evt.preventDefault())
// );

// loadForm.addEventListener("click", () => {
//   const url = document.getElementById("load").value;
//   console.log(url);
//   Player.load(url);
// });
